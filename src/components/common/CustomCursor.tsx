import React, { useEffect, useState } from 'react';
import { motion } from 'framer-motion';

export const CustomCursor: React.FC = () => {
  // Disabled custom cursor to match Letters design - using default cursor
  return null;
};
